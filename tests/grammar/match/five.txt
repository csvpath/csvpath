
[
    tally.levels(#level)
    @logbalance = above(@level.WARN, @level.DEBUG)
    and(last(), @logbalance.asbool) -> fail()
    and(last(), @logbalance.asbool) -> print("The warning and debugging messages are inverted
")
]


