[
                @m = mod(count_lines(), 2)
                @c = count( equals( @m, 0) )
                print.onmatch("printing: $.variables, count: $.match_count")
                ~ comment ~
            ]