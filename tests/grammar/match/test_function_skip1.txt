[
                @name = concat( #firstname, #lastname)
                skip(@name == "FishBat")
                push( "not_skipped", count_lines() )
                print("not skipped!")
            ]