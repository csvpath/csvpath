
    [
     tally.levels(#level)
     tally.hosts(#hostname)
     @error_count = @level.ERROR
     last() -> push("disp", @level.ERROR)
     last() -> push("disp", @level.WARN)
     last() -> push("disp", @level.DEBUG)
     @x = stdev(stack("disp"))
     last() -> print("$.variables.level, ERROR: $.variables.error_count, STDEV: $.variables.x")
    ]
